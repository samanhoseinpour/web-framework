{
  "name": "web-framework",
  "version": "1.0.0",
  "description": "In this repo, I create a client-side Framework similar to React, Vue, Angular or ... Maybe It's not gonna have the same functionality but It has a same purpose. It's going to fetch some data, render some content on web page and handle user events whenever user like messes with something.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:db": "json-server --watch db.json",
    "start:parcel": "parcel index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "json-server": "^0.17.3"
  }
}
